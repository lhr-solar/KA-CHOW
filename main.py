import time
class main:
    #high level idea: have an infinite (for the duration of the race) while loop 
    #that would update the car class every delta t time 
    #the if statements would be based on whatever research results we find on race strategy 
    deltaT = 5 #assuming simulator is updated every 5 secs
    while():
        if(deltaT==0):
            """
            
            """
            deltaT = 5
        else:
            deltaT = deltaT-1